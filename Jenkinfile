pipeline {
    sh "sudo chown jenkins: -R \$PWD/"
    agent { docker { image 'maven:3.3.3' } }
    stages {
        stage('build') {
            steps {
                sh 'mvn --version'
            }
        }
    }
}
